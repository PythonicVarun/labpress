name: Publish

on:
    push:
        tags: ["v*"]
    workflow_dispatch:
        inputs:
            dist-tag:
                description: "npm dist-tag to publish under"
                default: latest
                required: false

jobs:
    publish:
        runs-on: ubuntu-latest
        permissions:
            contents: write
            id-token: write
        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-node@v4
              with:
                  node-version: 24
                  registry-url: https://registry.npmjs.org

            - run: npm install -g npm@latest

            - run: npm install --no-audit --no-fund

            - name: Check the tag matches package.json
              if: startsWith(github.ref, 'refs/tags/')
              run: |
                  tagged="${GITHUB_REF_NAME#v}"
                  declared="$(node -p "require('./package.json').version")"
                  if [ "$tagged" != "$declared" ]; then
                      echo "Tag $GITHUB_REF_NAME does not match package.json version $declared" >&2
                      exit 1
                  fi

            - name: Smoke test the packed tarball
              run: |
                  tarball="$(npm pack --silent)"
                  mkdir -p /tmp/smoke && cd /tmp/smoke
                  npm init -y >/dev/null
                  npm install --no-audit --no-fund "$GITHUB_WORKSPACE/$tarball"
                  labpress() { ./node_modules/.bin/labpress "$@"; }

                  labpress --version
                  labpress themes >/dev/null

                  printf 'print("hi from labpress")\n' > hello.py
                  printf '%s' '{"cells":[{"cell_type":"code","execution_count":1,"metadata":{},"source":["print(\"from a notebook\")"],"outputs":[{"output_type":"stream","name":"stdout","text":["cells already ran\n"]}]}],"metadata":{},"nbformat":4,"nbformat_minor":5}' > notes.ipynb

                  # HTML is the build whose contents we can assert on.
                  labpress . --no-open --to html --out record.html
                  grep -q "hi from labpress" record.html
                  grep -q "cells already ran" record.html

                  # And the default, which drives the runner's Chrome.
                  labpress . --no-open --out record.pdf
                  [ -s record.pdf ]

            # Staged, not published
            - name: Stage the release on npm
              run: npm stage publish --provenance --access public --tag "${{ inputs.dist-tag || 'latest' }}"

            - name: Say how to approve it
              run: |
                  {
                      echo "### Staged, waiting for approval"
                      echo
                      echo "Nothing is installable yet. To finish the release:"
                      echo
                      echo '```bash'
                      echo "npm stage list labpress          # find the stage id"
                      echo "npm stage download <stage-id>    # check the tarball"
                      echo "npm stage approve <stage-id>     # publish it"
                      echo '```'
                      echo
                      echo "Changed your mind? \`npm stage reject <stage-id>\`."
                  } >> "$GITHUB_STEP_SUMMARY"

            - name: Create the GitHub release
              if: startsWith(github.ref, 'refs/tags/')
              env:
                  GH_TOKEN: ${{ github.token }}
              run: gh release create "$GITHUB_REF_NAME" --generate-notes --title "$GITHUB_REF_NAME"
